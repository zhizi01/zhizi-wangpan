{
  "name": "zhizi-files",
  "private": true,
  "description": "根目录聚合构建：产物在 dist/（后端）与 dist/app-dist/（前端）",
  "scripts": {
    "build": "npm run build:server && npm run build:web",
    "build:server": "npm run build --prefix server",
    "build:web": "npm run build --prefix web",
    "start": "node dist/index.js"
  }
}
